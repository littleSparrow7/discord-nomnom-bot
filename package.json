{
  "name": "discord-nomnom-bot",
  "version": "0.0.0",
  "private": true,
  "dependencies": {
    "node": "20.18.0"
  },
  "scripts": {
    "v": "node -v",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "test": "vitest"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.5.2",
    "wrangler": "^3.60.3",
    "vitest": "2.0.5"
  }
}
